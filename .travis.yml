language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t Karmantrex/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Karmnatrex/docker-react npm run test

