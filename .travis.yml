language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t karmantrex/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true karmantrex/docker-react npm run test

